# Security policy

Report workspace import bypasses, unsafe rendered content, or local-data loss
through GitHub private vulnerability reporting. Include a minimal sanitized
workspace export and reproduction steps.

PatchHive stores project context in the browser. Users should avoid pasting
secrets into missions or evidence and should review exported Markdown before sharing it.
