name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

jobs:
  verify:
    if: github.repository == 'Yuki9814/PatchHive'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      RELEASE_TAG: ${{ github.ref_name }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - name: Verify tag matches package version
        run: |
          [[ "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          test "$RELEASE_TAG" = "v$(node -p "require('./package.json').version")"
      - run: npm ci
      - run: npm run check
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
      - name: Package the verified Pages build
        run: |
          version="${RELEASE_TAG#v}"
          mkdir release-dist
          zip -qr "release-dist/patchhive-${version}-pages.zip" dist
          (
            cd release-dist
            shasum -a 256 "patchhive-${version}-pages.zip" > SHA256SUMS
          )
      - name: Upload verified release artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-dist
          path: release-dist/
          if-no-files-found: error
          retention-days: 1

  github-release:
    needs: verify
    if: github.repository == 'Yuki9814/PatchHive'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    environment: release
    env:
      RELEASE_TAG: ${{ github.ref_name }}
    steps:
      - name: Download verified release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-dist
          path: release-dist
      - name: Verify artifact names and checksums
        run: |
          [[ "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          version="${RELEASE_TAG#v}"
          archive="release-dist/patchhive-${version}-pages.zip"
          checksums="release-dist/SHA256SUMS"
          test -f "$archive"
          test -f "$checksums"
          test "$(find release-dist -maxdepth 1 -type f | wc -l | tr -d ' ')" = "2"
          (cd release-dist && shasum -a 256 -c SHA256SUMS)
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${RELEASE_TAG#v}"
          gh release create "$RELEASE_TAG" \
            "release-dist/patchhive-${version}-pages.zip" \
            "release-dist/SHA256SUMS" \
            --verify-tag \
            --title "$RELEASE_TAG" \
            --generate-notes
